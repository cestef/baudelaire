// Publishes as a flat `404.html`, the file a static host serves for an
// unmatched URL. Left out of the chapter order and the sitemap.
#let frontmatter = (
  title: "Not found",
)

That page isn't here. #link("/")[Back to the first chapter].
