#let frontmatter = (
  title: "Home",
  template: "layout.typ",
)

Welcome to your new Baudelaire site. Edit `content/index.typ` to change this
page, add posts under `content/posts/`, and style everything in
`assets/style.css`.

You may visit newly created posts #link("./posts/hello-world")[here], access it through various means by changing the `slug: <…>` frontmatter to a desirable redirect
