// Publishes as a flat `404.html`, the file a static host serves for an
// unmatched URL. Left out of the sidebar, the search index and the sitemap.
#let frontmatter = (
  title: "Not found",
)

That page isn't here. It may have moved, or the link may have been wrong.

- #link("/")[Home]
- #link("/guide/getting-started/")[Getting started]
