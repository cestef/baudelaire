name: Docs

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "src/**"
      - "install.sh"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".forgejo/workflows/docs.yml"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUSTFLAGS: -C link-arg=-fuse-ld=mold

jobs:
  ship:
    runs-on: linux-any-large
    steps:
      - uses: actions/checkout@v4

      - name: rust toolchain
        run: |
          if ! command -v rustup >/dev/null; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \
              | sh -s -- -y --default-toolchain none --profile minimal
            echo "$HOME/.cargo/bin" >> $GITHUB_PATH
            export PATH="$HOME/.cargo/bin:$PATH"
          fi
          rustup show active-toolchain || rustup toolchain install

      - name: linker (mold)
        run: |
          command -v mold >/dev/null || {
            sudo apt-get update
            sudo apt-get install -y --no-install-recommends mold
          }

      - name: cargo cache
        uses: https://github.com/Swatinem/rust-cache@v2
        with:
          shared-key: docs

      - name: build baudelaire
        run: cargo build --release

      - name: build docs site
        working-directory: docs
        run: ../target/release/baudelaire build

      - name: memory diagnostics
        if: failure()
        run: grep -H . /sys/fs/cgroup/memory.peak /sys/fs/cgroup/memory.max /sys/fs/cgroup/memory.events 2>/dev/null || true

      - uses: https://codeberg.org/git-pages/action@v2
        with:
          site: https://cstef.codeberg.page/baudelaire/
          token: ${{ forge.token }}
          source: docs/public/
