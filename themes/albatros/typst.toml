[package]
name = "albatros"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["cstef <root@cstef.dev>"]
license = "MIT"
description = "A centred, multilingual blog theme for baudelaire: light and dark, tags, reading time."
repository = "https://github.com/cestef/baudelaire"
keywords = ["baudelaire", "theme", "blog", "multilingual", "html"]
