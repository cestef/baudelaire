[package]
name = "phares"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["cstef <root@cstef.dev>"]
license = "MIT"
description = "A documentation theme for baudelaire: sidebar, search palette, on-page contents."
repository = "https://github.com/cestef/baudelaire"
keywords = ["baudelaire", "theme", "documentation", "html"]
