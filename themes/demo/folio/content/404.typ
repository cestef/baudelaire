// Publishes as a flat `404.html`, the file a static host serves for an
// unmatched URL.
#let frontmatter = (
  title: "Not found",
  template: "not-found.typ",
)

Nothing is published at that address. The work is all under one directory, and
the grid below leads back to it.
